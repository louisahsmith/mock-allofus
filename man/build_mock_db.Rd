% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_mock_db.R
\name{build_mock_db}
\alias{build_mock_db}
\title{Build a local mock All of Us DuckDB database}
\usage{
build_mock_db(
  path = default_mock_db_path(),
  n_persons = 2000L,
  seed = 20240101L,
  survey_questions = NULL,
  overwrite = FALSE,
  quiet = FALSE
)
}
\arguments{
\item{path}{File path for the DuckDB database. Defaults to a cached location
under \code{\link[tools:userdir]{tools::R_user_dir()}}.}

\item{n_persons}{Number of synthetic participants to generate. Default 2000.}

\item{seed}{Random seed for reproducible generation. Default 20240101.}

\item{survey_questions}{Optional integer vector of survey question concept
ids (from \code{allofus::aou_codebook}) to populate in \code{observation}. Defaults
to a sample that includes the vignette questions (gender \code{1585838},
birthplace \code{1586135}).}

\item{overwrite}{Overwrite an existing database file. Default \code{FALSE}
(an existing file at \code{path} is left untouched and its path returned).}

\item{quiet}{Suppress progress messages.}
}
\value{
The \code{path} to the built database, invisibly.
}
\description{
Creates a DuckDB database file whose tables and columns mirror the All of Us
Curated Data Repository (OMOP CDM core tables plus All of Us extension and
cohort-builder tables), populated with fully synthetic, randomly generated
data. The data does \strong{not} reflect real All of Us data and results are not
meant to be realistic; the purpose is to let \code{allofus} analysis code run and
be developed locally.
}
\details{
All tables in the All of Us data dictionary are created (so table references
resolve); a core subset is populated with data:
\code{person}, \code{concept}, the clinical domain tables and their \verb{_ext} tables,
survey rows in \code{observation}, \code{observation_period}, \code{cb_search_person},
\code{ds_survey}, and \code{survey_conduct}. The clinical concept ids used in the
\code{allofus} vignettes are included so the documented workflows return data.

Use \code{\link[=mock_seed_concept_set]{mock_seed_concept_set()}} / \code{\link[=mock_seed_survey]{mock_seed_survey()}} afterwards to guarantee
coverage of the specific concepts your own study queries.
}
