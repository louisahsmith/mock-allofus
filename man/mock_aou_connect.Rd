% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mock_aou_connect.R
\name{mock_aou_connect}
\alias{mock_aou_connect}
\title{Connect to the local mock All of Us database}
\usage{
mock_aou_connect(
  path = default_mock_db_path(),
  cdr = "main",
  quiet = FALSE,
  ...
)
}
\arguments{
\item{path}{Path to the DuckDB database file. Defaults to the cached location
(\code{\link[=default_mock_db_path]{default_mock_db_path()}}); built with \code{\link[=build_mock_db]{build_mock_db()}} if absent.}

\item{cdr}{The schema name used for \code{{CDR}} interpolation in \code{allofus} SQL
helpers. Defaults to \code{"main"} (DuckDB's default schema, where the mock
tables live), so references like \code{`{CDR}.person`} resolve.}

\item{quiet}{Suppress the connection message.}

\item{...}{Further arguments passed to \code{\link[DBI:dbConnect]{DBI::dbConnect()}}.}
}
\value{
A DuckDB connection object (\code{duckdb_connection}), also stored in
\code{getOption("aou.default.con")}.
}
\description{
Opens (building first if necessary) the local mock DuckDB database and wires
it into the \code{allofus} package the same way \code{\link[allofus:aou_connect]{allofus::aou_connect()}} does on
the Researcher Workbench: the connection is stored in
\code{getOption("aou.default.con")} and a curated-data-repository name in
\code{getOption("aou.default.cdr")}. After calling this, \code{allofus} analysis code
that relies on those defaults runs against the mock database unchanged.
}
\details{
BigQuery-compatibility macros are registered on the connection (see
\code{\link[=register_bq_macros]{register_bq_macros()}}) so SQL emitted by \code{allofus} in the BigQuery dialect
executes on DuckDB.
}
\examples{
\dontrun{
con <- mock_aou_connect()
# the same code you would run on the Workbench:
dplyr::tbl(con, "person")
}
}
